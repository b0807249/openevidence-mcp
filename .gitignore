node_modules/
dist/
.omx/
.antigravitycli/
.env
.env.local

# local auth/session artifacts
.openevidence-mcp/
auth/
cookies.json
cookies.*.json
storage-state.json
openevidence-fingerprint*.json
*.har

# extension signing key — controls the extension's identity; never commit
*.pem
extension/key.pem

# packaged skill bundles may carry cookies — keep local
*.skill

# OS/editor
.DS_Store
Thumbs.db
*.log
