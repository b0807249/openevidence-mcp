{
  "manifest_version": 3,
  "name": "OpenEvidence MCP Relay",
  "version": "0.1.0",
  "description": "Runs OpenEvidence ask POSTs inside your real logged-in tab so the local MCP server never hits DataDome. Localhost-only.",
  "permissions": ["scripting", "tabs", "storage", "alarms"],
  "host_permissions": [
    "https://www.openevidence.com/*",
    "http://127.0.0.1/*",
    "http://localhost/*"
  ],
  "background": {
    "service_worker": "background.js"
  }
}
